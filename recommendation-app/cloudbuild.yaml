steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-t", "us-west2-docker.pkg.dev/$PROJECT_ID/cart-app/recommendation-app:latest", "recommendation-app"]
images: ["us-west2-docker.pkg.dev/$PROJECT_ID/cart-app/recommendation-app:latest"]
options:
  requestedVerifyOption: VERIFIED
  logging: CLOUD_LOGGING_ONLY

